# 보험찾개냥 Landing_Page

반려동물 보험 대리청구 서비스의 웹사이트. 랜딩(`/`)과 대리청구 신청 창구(`/apply`)를 한 Vite 앱이 서빙한다.

| 경로 | 내용 |
| --- | --- |
| `Project/` | Vite + React 19 + TypeScript. `npm` 명령은 전부 이 안에서 실행한다 |
| `Project/api/` | Vercel 서버리스 함수. 비밀(`NOTION_TOKEN` 등)은 여기와 `Project/server/`에서만 읽는다 |
| `Project/server/` | 서버리스 함수와 로컬 dev 미들웨어(`vite.config.ts`)가 같이 쓰는 순수 로직 |
| `docs/spec/` | `SSH-XXX/{spec,tasks}.md` + 검증 스크린샷 |
| `.claude/` | 스킬(`kickoff` `commit` `pr` `pr-review`) · 리뷰 에이전트 · 커밋 전 검증 훅 |

## 명령 — 전부 `Project/`에서

```bash
npm run dev       # 로컬. /api/* 는 vite.config.ts 미들웨어가 server/ 로직을 그대로 부른다
npm run lint      # oxlint
npm run build     # tsc -b && vite build — 타입 검사는 여기 포함된다
npm test          # node --test (Node 26이 TS를 그대로 실행한다. 별도 러너 없음)
```

CI(`web-ci.yml`)는 위 `lint` · `build` · `test`를 **같은 명령으로** 돌린다. 로컬에서 초록이면 CI도 초록이다.

---

## 절대 금지

1. **커밋·PR 본문에 AI 서명을 넣지 않는다.** `Co-Authored-By`, `Claude-Session`, `Generated with Claude Code` — 어떤 형태도.
   CI(`rules-ci.yml`)가 막는다.
2. **`dev`·`main`에 직접 커밋·푸시하지 않는다.** 항상 `<type>/SSH-<번호>` 브랜치에서.
3. **머지하지 않는다.** PR 생성·본문 갱신까지가 에이전트의 일이고, Squash Merge는 사람이 한다.
4. **리뷰를 요청하는 전환은 사람이 한다.** GitHub **Draft → Ready**(`gh pr ready`)도, Jira **진행 중 → 검토 중**도.
   "이제 남이 시간을 써서 봐도 된다"는 선언이고 그 판단은 만든 사람이 내린다. Jira 검토 중은 Slack으로
   자동 발송된다. 에이전트는 **무엇이 남았는지 보고까지만** 한다.

## 애매하면 묻는다

판단이 갈리는 지점에서는 **멈추고 물어본다.** 혼자 정하고 나중에 통보하지 않는다.
단, **코드나 문서를 읽으면 답이 나오는 것은 묻지 않는다.** 먼저 읽고, 그래도 안 나오면 묻는다.

## 작업 흐름

**사람 리뷰는 두 번이다.** PR 하나를 Draft로 먼저 열고(1차: 작업 계획), 개발이 끝나면 사람이 Ready로
바꾼다(2차: 결과물). 그 사이에 **AI 리뷰(`/pr-review`)가 한 번** 들어간다.

1. **Jira 티켓** — 백로그는 Jira(SSH 프로젝트) 하나다. 티켓 번호로 시작한다
2. **브랜치** — `git switch -c <type>/SSH-<번호> origin/dev`
3. **문서** — 위키(`llm-wiki`, 로컬 경로는 `CLAUDE.local.md`)에서 기획·설계를 먼저 읽고 `docs/spec/SSH-<번호>/`에
   `spec.md`·`tasks.md`를 쓴다. **참고한 위키 문서·Figma 노드·형제 티켓을 spec 「배경」에 적는다.**
   `/kickoff` 스킬이 여기까지 한다
4. **Draft PR 생성 → ⏸ 1차 리뷰(작업 계획).** Draft를 만든 뒤에는 **멈추고 리뷰를 기다린다.** 이어서 개발하지 않는다
5. **리뷰 반영 후 개발** — 같은 브랜치·같은 PR. `tasks.md` 순서대로, 의미 단위로 커밋(`/commit`)
6. **개발을 마치면 `/pr`로 본문 갱신 → `/pr-review`** — **반드시 별도 서브에이전트로.** 구현한 세션이 자기 코드에
   도장을 찍으면 리뷰가 아니라 자기 확인이 된다. P1·P2는 등급대로 반영하고, P3 이하는 보고만.
   **한 PR에 리뷰를 두 번째로 등록하기 전에는 사람에게 확인받는다.** 무엇이 남았는지 보고 — **여기까지가 에이전트의 마지막 일**
7. **Ready 전환 + Jira 검토 중** (사람) → ⏸ 2차 리뷰
8. **Squash Merge → `dev`** (사람)

**spec·tasks 골격** — 본보기는 `docs/spec/SSH-542/`.
- `spec.md`: 한 줄 완료 기준 → 배경(참고 문서·지금 레포 상태·왜 이 방식인가) → 범위(절 번호) → 범위 밖(표: 안 하는 것 | 어디서) → 검증(체크박스) → N차 리뷰 결정(날짜)
- `tasks.md`: `0. 문서·Draft PR` → `1..N` 구현 절(spec 절 번호와 맞추고 **커밋 메시지를 명기**) → 검증 → 마무리 → 하지 않는 것.
  체크박스가 재개 지점이다 — 새 세션은 `tasks.md`의 끊긴 자리부터 잇는다

## `dev` → `main`

`dev`가 base다. 프로덕션(`main`) 반영은 사람이 `dev → main` PR을 열어 머지한다. 시점도 사람이 정한다.
그 PR은 제목이 자유이고 규칙 CI가 서명 검사만 한다. 에이전트는 `dev → main` PR을 만들지 않는다.

## 브랜치 · 커밋 · PR

- **브랜치** — `<type>/SSH-<번호>` (예: `feat/SSH-543`). base는 항상 최신 `origin/dev`.
  이슈 키가 브랜치명에 있어야 Jira 「개발」 패널에 자동 연결된다
- **커밋** — `<type>: <제목>`. 한 줄, 100자 이내, 한국어. **이슈 키를 붙이지 않는다.** 한 커밋에 목적 하나
  - type: `feat` `fix` `chore` `style` `refactor` `docs` `test` (브랜치와 동일)
- **PR 제목** — `작업한 내용(SSH-번호)` (예: `/apply 진입점 + 페이지 셸 구성(SSH-542)`). 스코프 없음 — 스택이 하나다.
  Squash Merge하면 이 제목이 `dev` 커밋 제목이 된다
- **한 PR에는 티켓 하나.** 줄 수 제한은 없다. 대신 **변경된 파일마다 "왜 이 티켓에 필요한가"를 한 줄로 답할 수 있어야 한다.**
  답이 안 나오는 파일이 범위 밖이다 — 눈에 띄어서 고친 무관한 버그, 요청 없던 리팩터링, "하는 김에" 정리
- **UI가 바뀐 PR에는 스크린샷 3장** — **1440 · 768 · 390** 폭. 대표 상태는 그 화면에 처음 들어갔을 때의 모습.
  `docs/spec/SSH-<번호>/`에 커밋하고 PR 본문에 임베드한다(`pr` 스킬). Vercel 프리뷰 링크도 함께
- **큰 작업은 나눈다.** 티켓을 쓸 때가 가장 싸다 — 완료 기준을 "그리고" 없이 한 문장으로 못 쓰면 쪼갠다.
  각 PR은 머지된 뒤 `dev`가 ① 빌드되고 ② 뜨고 ③ 남긴 것을 한 문장으로 설명할 수 있어야 한다.
  앞 PR이 머지된 뒤 최신 `dev`에서 다음 브랜치를 딴다 — **PR을 쌓지 않는다**(Squash Merge라 base가 어긋난다)

## 작업 원칙

- 작업 전 관련 코드와 기존 패턴을 먼저 본다. **기존 코드가 이 문서보다 우선이다**
- 요청 범위 밖의 리팩터링·파일 변경은 하지 않는다
- 의존성은 꼭 필요할 때만 추가하고, **추가 이유를 PR 본문에 적는다**
- 변경한 영역에 맞는 lint·테스트를 실행한다. **실행하지 못한 검증이 있으면 이유를 적는다**

## 웹 스택 규칙 — 리뷰(`pr-reviewer`)의 근거

기존 코드에서 도출한 것이다. 여기 없는 영역은 정하지 않은 것이지 빠뜨린 것이 아니다.

- **라우터 라이브러리를 쓰지 않는다.** 페이지는 `/`와 `/apply` 둘이고 `src/lib/route.ts`의 `pathname` 분기로 가른다.
  단계(S1~S6)는 `/apply` 안의 상태이지 URL이 아니다
- **스타일** — `src/index.css`의 토큰(`--primary-500`, `--radius-md` …)·공용 클래스(`.wrap` `.btn`)를 재사용한다. 폼 입력은 `src/styles/apply.css`의
  `.apply-field`·`.apply-input`(랜딩의 `.field`는 사전 알림 폼과 함께 SSH-545에서 지웠다).
  기능 전용 CSS는 별도 파일(`src/styles/<기능>.css`)에 **접두사 클래스**(`apply-…`)로. 색·간격을 하드코딩하지 않는다
- **상태** — 순수 reducer(`src/apply/state.ts` 패턴, React 의존 없음) + Context Provider. 순수 함수는 `node:test`로 테스트한다.
  테스트 파일(`*.test.ts`)은 `tsconfig.app.json`에서 빼고 `tsconfig.node.json`에 넣는다 — `tsc -b`가 타입 검사한다
- **비밀** — `NOTION_TOKEN` 같은 값은 `server/`·`api/`에서만 `process.env`로 읽는다. **`VITE_` 접두사 변수는 브라우저 번들에
  들어간다** — 공개해도 되는 값만. 새 변수는 이름만 `Project/.env.example`에 추가한다
- **서버리스 함수(`api/*.ts`)** — 요청 파싱·응답 변환만 하고 로직은 `server/`에 둔다(로컬 dev 미들웨어와 공유).
  입력은 검증하고, 실패는 `{ ok: false, error }` JSON으로. import는 `.js` 확장자(nodenext)
- **SEO** — `index.html` 한 파일이 두 페이지를 서빙한다. 페이지별 메타는 런타임 교체(`useApplyMeta.ts` 패턴)
- **트래킹** — `src/lib/analytics.ts`의 `track()`만 쓴다. 이벤트 이름은 기존 목록을 따른다

## 보안

**이 저장소는 public이다.** 커밋·PR·CI 로그·스크린샷이 전부 공개된다.

- 환경 변수·API 키·토큰을 **코드와 커밋에 넣지 않는다.** `.env`는 수정하지 않는다
- **문서·PR 본문·스크린샷·로그에도 실제 값을 넣지 않는다.** Figma 파일 키, 데이터소스 ID, 실제 사용자 데이터, 계정 이메일 포함
- 예시가 필요하면 명백한 더미를 쓴다 (`https://api.example.com`, `user@example.com`)

## 저 문맥(Low Context) 원칙

읽는 사람이 사전 지식 없이 들어온다고 가정하고 문맥을 전부 적는다. PR 본문·주석·커밋 메시지 전부.
**6개월 뒤의 내가 독자다.** 대화로 설명한 의도는 코드에 남지 않는다 — 설명이 필요하면 PR 본문에 쓴다.

## 순리

"if 문 하나면 되는데"에 타협하지 않는다. 지금 동작하지만 다음 작업에서 같은 임시방편을 반복하게 만드는 코드는 부채다.
타협했다면 PR 본문에 적는다. **적으면 부채고, 안 적으면 사고다.**

## 산출물

커밋 메시지, PR 본문, 주석, 보고 — **전부 한국어로 쓴다.**
